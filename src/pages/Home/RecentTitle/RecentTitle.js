import React from "react";

const RecentTitle = ({ title }) => {
	return (
		<h3 className="text-left text-lg pl-2 py-2 border-b-2 border-x-red-50 text-white">
			{title}
		</h3>
	);
};

export default RecentTitle;
